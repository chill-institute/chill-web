name: Main

on:
  push:
    branches:
      - main

jobs:
  verify:
    concurrency:
      group: web-main-verify-${{ github.ref }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          version: "0.1.14"
          node-version-file: ".node-version"
          cache: true
          run-install: false

      - name: Install dependencies
        run: vp install

      - name: Run core verify checks
        run: vp run verify

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: web-dist
          path: dist
          if-no-files-found: error

  e2e:
    needs: verify
    concurrency:
      group: web-main-e2e-${{ github.ref }}
      cancel-in-progress: true
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: web-dist
          path: dist

      - name: Set up Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          version: "0.1.14"
          node-version-file: ".node-version"
          cache: true
          run-install: false

      - name: Install dependencies
        run: vp install

      - name: Install Playwright browser
        run: vp exec playwright install --with-deps chromium

      - name: End-to-end tests
        run: vp run e2e

      - name: Upload Playwright artifacts
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-artifacts-e2e
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore

  deploy:
    needs: [verify, e2e]
    if: ${{ needs.verify.result == 'success' && needs.e2e.result == 'success' }}
    concurrency:
      group: web-deploy-production
      cancel-in-progress: false
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: web-dist
          path: dist

      - name: Set up Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          version: "0.1.14"
          node-version-file: ".node-version"
          cache: true
          run-install: false

      - name: Install dependencies
        run: vp install

      - name: Deploy production to Cloudflare Pages
        uses: ./.github/actions/cloudflare-pages-deploy
        with:
          api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          account-id: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          project-name: web
          branch: main

      - name: Publish summary
        run: |
          {
            echo "## Web deploy"
            echo
            echo "- Environment: \`production\`"
            echo "- Project: \`web\`"
            echo "- URL: [https://chill.institute](https://chill.institute)"
            echo "- Flow: \`push Main workflow\`"
          } >> "${GITHUB_STEP_SUMMARY}"
